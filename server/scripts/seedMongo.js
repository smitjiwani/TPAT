import Quiz from "./quizSchema"
import mongoose form 'mongoose'

const seedMongo = async () => {
    try {
        const quiz1 = new Quiz({
            name: "username",
            questionList: {
                queNum: 1,
                question: "NiggaBalz"
            }
        })
    }
}
